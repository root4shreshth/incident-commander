__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv/
venv/
*.log
outputs/
.claude/

# Recorded trained-agent runs (large, regenerable from notebook)
runs/

# MS Office lock files
~$*

# Local site target for sim-to-real demo (vibecoded separately)
targets/

# Uploaded codebases (Phase 3 ZIP upload destination)
uploaded_codebase/

# Local dev caches
.pytest_cache/
.ruff_cache/

# Generated / non-canonical docs (the canonical write-up is README.md)
*.docx
docs/
PRODUCT_DOCUMENT.md
coreideation.md

# Intermediate JSONL outputs from scripts/export_trajectories.py (regenerable;
# the curated parquet/jsonl artifacts in results/hf_dataset/ ARE committed)
results/dataset_runs/

# Local screenshots and the hackathon's external instruction PDFs (we keep
# them locally for reference but they're not part of the submission).
127.0.0.1_*.png
*.png
"[External] *.pdf"

